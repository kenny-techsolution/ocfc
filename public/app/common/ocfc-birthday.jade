.birthday-widget(ng-repeat='user in users |filter:comingBirthdays')
	img(ng-src='{{user.userId.profileImg}}')
	.name {{user.userId.fullName|capitalize}}
	.glyphicon.glyphicon-gift
	.date {{user.userId.birthday|date:'MMMM d'}}